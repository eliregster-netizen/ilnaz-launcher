{
  "build": {
    "rootDirectory": "server",
    "buildCommand": "npm ci --omit=dev"
  },
  "deploy": {
    "startCommand": "node index.js",
    "healthcheckPath": "/api/music",
    "restartPolicyType": "ON_FAILURE"
  }
}

}
